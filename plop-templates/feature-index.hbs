/**
 * {{pascalCase name}} Feature
 *
 * Public surface for the {{kebabCase name}} feature module. Add re-exports
 * here as the feature grows; consumers should import from this barrel only.
 */

export {}
